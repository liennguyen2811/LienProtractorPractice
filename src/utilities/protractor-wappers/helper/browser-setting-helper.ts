import DriverProperty from "../driver/driverproperty";

export default class BrowserSettingHelper{
    public static getDriverProperty(file: string, sectionName: string, testName: string){
        let property: DriverProperty = new DriverProperty();
        
    }
}